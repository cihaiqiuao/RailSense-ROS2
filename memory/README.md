# TrainROS 记忆目录

本目录使用三层 Markdown 结构保存项目内记忆。

## 结构

- `Codex.md`：主索引，控制在 200 行内。
- `projects/`：长期项目详情。
- `decisions/`：带日期的架构和实现决策。
- `handoffs/`：长对话或上下文接近一半时的交接摘要。
- `archive/`：过期或重复笔记。

使用时先读 `Codex.md`，只有当前任务需要更多细节时再打开其他文件。
